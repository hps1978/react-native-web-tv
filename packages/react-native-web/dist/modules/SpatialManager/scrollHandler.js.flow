/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict
 * @format
 */
import { getCurrentTime, scheduleAnimationFrame, cancelScheduledFrame, findScrollableAncestor, isElementInWindowViewport, getElementVisibilityRatio, inferScrollDirection, getBoundingRectangles, getAxisScrollDelta } from './utils';
import type { ElemData, ScrollableAncestorInfo } from './utils';
type SpatialScrollConfigType = {
  leftEdgePaddingPx?: number,
  topEdgePaddingPx?: number,
  scrollThrottleMs?: number,
  // not used for now
  smoothScrollEnabled?: boolean,
  scrollAnimationDurationMsVertical?: number,
  scrollAnimationDurationMsHorizontal?: number,
};
type ResolvedSpatialScrollConfigType = {
  leftEdgePaddingPx: number,
  topEdgePaddingPx: number,
  scrollThrottleMs: number,
  smoothScrollEnabled: boolean,
  scrollAnimationDurationMsVertical: number,
  scrollAnimationDurationMsHorizontal: number,
};
type ScrollStateType = {
  scrollAnimationFrame: number | null
};
type GetCurrentFocusType = () => {
  elem: HTMLElement | null,
  parentContainer: HTMLElement | null,
};
type OnScrollRefocusType = (params: {
  currentFocus: ElemData,
  scrollContainer: HTMLElement | null,
}) => void;
type ScrollContainerOptions = {
  horizontal?: boolean,
  pageSize?: number,
  scrollDurationMs?: number,
  snapToStart?: boolean,
  snapToEnd?: boolean,
};
const DEFAULT_SPATIAL_SCROLL_CONFIG: ResolvedSpatialScrollConfigType = {
  leftEdgePaddingPx: 0,
  // only used on the left edge and in horizontal scrolling
  topEdgePaddingPx: 0,
  // only used on the top edge and in vertical up scrolling
  scrollThrottleMs: 80,
  // not used for now
  smoothScrollEnabled: false,
  scrollAnimationDurationMsVertical: 0,
  scrollAnimationDurationMsHorizontal: 0
};

// Singleton class to manage scroll operations
declare class ScrollHandler {
  _isConfigured: boolean,
  _scrollConfig: ResolvedSpatialScrollConfigType,
  _focusMode: 'AlignLeft' | 'default',
  _scrollState: ScrollStateType,
  _isSpatialManagerInitiatedScroll: boolean,
  _reacquireFocusTimeout: TimeoutID | null,
  _hasScrollEndEvent: boolean,
  constructor(): any,
  setupScrollHandler(config?: {
    scrollConfig?: SpatialScrollConfigType,
    focusMode?: 'AlignLeft' | 'default',
    scrollState?: ScrollStateType,
  }): void,
  animateScrollTo(scrollable: any, isVertical: boolean, nextOffset: number, durationMs: number): any,
  performScroll(scrollable: any, isVertical: boolean, nextOffset: number, liveNextOffset: number, animate: boolean): any,
  scrollAxis(scrollable: any, isWindowScroll: boolean, isVertical: boolean, scrollDelta: number, animate: boolean): void,
  scrollContainer(scrollable: HTMLElement | null, keyCode: string, options?: ScrollContainerOptions): boolean,
  scrollToAlignLeft(elemData: ElemData, keyCode: string, currentElemData: ElemData | null, verticalScroll: ScrollableAncestorInfo, horizontalScroll: ScrollableAncestorInfo): any,
  scrollToElement(nextElemData: ElemData, currentElemData: ElemData | null, keyCode: string): any,
  setupAppInitiatedScrollHandler(container: HTMLElement | Document, getCurrentFocus: GetCurrentFocusType, onScrollRefocus: OnScrollRefocusType): () => void,
  scrollToEdge(elem: HTMLElement | null, keyCode: string): any,
}
export type { SpatialScrollConfigType, GetCurrentFocusType, OnScrollRefocusType, ElemData, ScrollContainerOptions };
const scrollHandler = new ScrollHandler();
declare export var setupScrollHandler: (config: any) => any;
declare export var setupAppInitiatedScrollHandler: (container: any, getCurrentFocus: any, onScrollRefocus: any) => any;
export { isElementInWindowViewport };
declare export var scrollToEdge: (elem: any, keyCode: any) => any;
declare export var scrollToElement: (nextElemData: any, currentElemData: any, keyCode: any) => any;
declare export var scrollContainer: (scrollable: any, keyCode: any, options: any) => any;